# This plugin is a fork of bookshelf.koplugin by AndyHazz, whose work is the
# larger part of it by a wide margin. Both are listed, upstream first, because
# forking someone's project should not quietly redirect the support for it.
#
# Ko-fi only for the fork: GitHub Sponsors pays out through Stripe, which does
# not operate in Vietnam, so a `github:` entry would draw a button that cannot
# receive anything.
ko_fi: andyhazz
custom: ["https://ko-fi.com/hhoangg"]
