local _ = require("gettext")
return {
    fullname = _("Kindle-style UI"),
    description = _([[Splits the reader's overloaded swipe-down into the three panels Kindle uses: a control centre from the top edge, the page browser from the bottom, and reading controls from the toolbar.]]),
}
